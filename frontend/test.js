const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('LOG:', msg.text())); page.on('pageerror', err => console.log('ERR:', err)); await page.goto('http://127.0.0.1:8080'); await page.waitForTimeout(1000); console.log('clicking...'); await page.evaluate(() => document.querySelector('.landing-btn-primary').click()); await page.waitForTimeout(1000); await browser.close(); })();
